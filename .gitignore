# This repo's main branch hosts only the README + LICENSE.
# Everything else is generated by the plugin (in gh-pages branch) or kept
# locally (config, state, generated artifacts). Ignore everything by default
# and explicitly allow only the few files we publish.

*
!.gitignore
!README.md
!LICENSE
